import { ChangeDetectionStrategy, Component } from '@angular/core';

/**
 * Inline form row container overlay.
 *
 * Placeholder component — implementation pending.
 */
@Component({
  selector: 'lw-data-table-active-row',
  standalone: true,
  templateUrl: './data-table-active-row.component.html',
  styleUrl: './data-table-active-row.component.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class DataTableActiveRowComponent {}
